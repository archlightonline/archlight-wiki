# esbuild ships its platform binary via an optional dependency, so its (blocked)
# postinstall build script is not required — verified working. Disable pnpm's
# pre-run dependency verification so `pnpm <script>` does not fail on the
# "ignored builds" warning.
verify-deps-before-run=false
