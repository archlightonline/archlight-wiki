# Approved Concepts

Move concept pages here only after they match the approved template shell and pass review.

Approved means:

- header/nav/section shell matches template
- tables are consistent
- media is handled consistently
- normal article text is readable
- custom blocks are used only where needed
- no shell overrides were added
