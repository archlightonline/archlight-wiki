[build]
builder = "nixpacks"
buildCommand = "pnpm install --frozen-lockfile && pnpm build"

[deploy]
startCommand = "node --experimental-vm-modules node_modules/.bin/tsx server/index.ts"
healthcheckPath = "/api/trpc/pages.siteStats"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
