[build]
builder = "nixpacks"
buildCommand = "npm install -g pnpm@9.15.4 && pnpm install --frozen-lockfile && pnpm build"

[deploy]
startCommand = "pnpm start"
healthcheckPath = "/api/trpc/pages.siteStats"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
